<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="in.macrocodes.bloodbankmanage">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BloodBankManage">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DonateBlood" />
        <activity android:name=".RequestBlood" />
        <activity android:name=".UserMainPage" />
        <activity android:name=".HospitalMainPage" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".LoginActivty" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>